export const updateOrganization = (organization) => ({
  type: 'UPDATE_ORGANIZATION',
  organization
});

export const validateOrganization = (organization) => ({
  type: 'VALIDATE_ORGANIZATION',
  organization
});

export const updateArticle = (article) => ({
  type: 'UPDATE_ARTICLE',
  article
});

export const validateArticle = (article) => ({
  type: 'VALIDATE_ARTICLE',
  article
});

export const updateAnnotationType = (annotationType) => ({
  type: 'UPDATE_ANNOTATION_TYPE',
  annotationType
});
